{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h4>{{ 'Intervention creation'| trans }}</h4>
        <p>L'intervention étant entrée à la journée, pensez à scinder par jour, notamment quand il s'agit de congés, n'entrez pas directement 37 heures par exemple, faites autant d'interventions que de journées.</p>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-success" type="submit" value="{{ 'Create'| trans }}" />
        {{ form_end(form) }}
        <a class="btn btn-success" href="{{ path('intervention_index') }}">{{ 'Back to the list'| trans }}</a>
    </div>

{% endblock %}